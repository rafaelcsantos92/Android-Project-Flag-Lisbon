<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="movies.flag.pt.moviesapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="DATABASE"
            android:value="MoviesApp_db.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="movies.flag.pt.moviesapp" />


        <activity
            android:name=".screens.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.FirstScreen" />
        <activity android:name=".screens.ExampleScreen" />
        <activity android:name=".screens.MoviesScreen" />
        <activity android:name=".screens.MovieDetailsScreen" />
        <activity android:name=".screens.TvPopularDetailsScreen" />
        <activity android:name=".screens.MainScreen" />
        <activity android:name=".screens.TvPopularScreen" />

    </application>

</manifest>
